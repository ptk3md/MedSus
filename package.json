{
  "name": "sismed-typescript-clone",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsc && node scripts/copy-static.mjs",
    "dev": "tsc --watch",
    "preview": "npx serve dist",
    "clean": "node scripts/clean.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
